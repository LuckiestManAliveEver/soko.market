# Domain modularization roadmap

## Why this exists

`services/api/src/cp2/store.ts` is a single class (`Cp2Store`) backing 128
entity `Map`s (70 remaining after the commerce, compliance, logistics,
suppliers/sales-agents/receipts, document-imports, notifications,
network/contacts-graph, and messaging/channels extractions below) behind
one shared auth gate, one generic in-memory snapshot/restore cycle, one
generic Postgres persistence sweep (`postgres-store.ts`'s
`normalizedCollections`),
and one account-deletion/GDPR-purge sweep. At ~27,000 lines it is the
single largest deviation from a one-concern-per-directory architecture in
this codebase, and the same is true in a smaller way of
`apps/web/src/SokoApplication.tsx` (~22,900 lines) on the frontend.

**True multi-service separation (independent deploy unit, independent
persistence) is not what this roadmap proposes.** That would require
re-architecting the shared persistence and auth layer first - a
foundational rewrite touching all 70 remaining entity types, with a
regression blast radius covering the entire product. This roadmap is
instead **in-process domain modularization**: splitting `store.ts` into
per-domain classes composed together inside the same `Cp2Store`, each
owning its own `Map`s and business logic, with explicit constructor-
injected dependencies instead of ambient `this.*` reach into the rest of
the store. This delivers the real value CLAUDE.md's services-first
principle is after - sharp per-concern boundaries, a file you can actually
review and reason about, less risk of two changes in different domains
colliding - without pretending the persistence/auth coupling isn't there.

## The pattern (established by the first slice)

See `services/api/src/cp2/domains/commerce/` for the reference
implementation: `store.ts` (a `CommerceDomain` class owning
`product_capture_jobs`/`status_broadcasts`/`buy_orders`/`status_orders`/
`unified_checkouts`), `shared.ts` (domain-local pure helpers), and a
`CommerceDomainDeps` interface listing every cross-domain dependency
explicitly (auth checks, product/business/network/customer/user lookups,
invoice building). `Cp2Store` constructs it once and delegates its public
methods to it with identical signatures - a zero-behavior-change
extraction, verified by the full test suite (in-memory) and the live-
Postgres restart-persistence test (`tests/cp2-postgres-store.test.ts`)
staying green before and after.

Two small supporting extractions made this safe rather than circular:
`cp2-error.ts` (the `Cp2Error` class, later joined by the `assertValid`
helper the compliance slice needed) and `text-normalization.ts` (two
generic string validators) - both pulled out of `store.ts` first, since a
domain module throwing/validating with them would otherwise need a
circular value-import back into `store.ts`. Both are re-exported from
`store.ts` for existing external consumers (routes.ts, tests), so nothing
outside this refactor needed to change. Any future domain extraction that
needs another store.ts-local helper should follow the same move-it-out-
first pattern rather than importing back into store.ts - check
`cp2-error.ts` first, since generic validation/error helpers belong there
now, not duplicated per domain.

Every extraction should also touch the same four generic sweep sites in
`store.ts` for its own Maps: `Cp2Snapshot` (add fields), `snapshot()`
(serialize), `hydrateSnapshot()` (clear + repopulate - **the first slice
found and fixed a real bug here**: `statusBroadcasts`/`buyOrders`/
`statusOrders`/`unifiedCheckouts` were missing from the clear step before
this roadmap's own first pass caught it, meaning a live resync could have
left stale entries behind), and the account-deletion sweep
(`deleteScopedMapRecords` calls). Also check `postgres-store.ts`'s
`normalizedCollections`/`emptySnapshot()`/column-mapping candidate lists
(`firstText(...)` calls) and `scripts/purge-all-users.sql`'s table
classification - all of which the commerce slice needed and are easy to
forget, per this session's own experience fixing exactly this class of gap
for the same five tables after the fact.

## Candidate domains, sequenced low-coupling first

Coupling here means "how many Maps/methods outside the domain does it need
to read or call" - roughly what `CommerceDomainDeps` would look like for
each. Higher-coupling domains are listed later not because they matter
less, but because extracting them safely requires the pattern to be
proven on easier cases first.

**Correction from the second slice (Compliance/Beta/Launch, done):** this
table's coupling estimates were written from a signature-level grep, not
from reading the method bodies. Domain 1 turned out to have much higher
coupling than "Low" - `buildComplianceReport`/`buildBetaReadinessReport`/
`buildLaunchReadinessReport` (the readiness-dashboard builders) reach into
invoices, payments, sync queue, offline cache, products, customers,
logistics, data exports, and account-deletion requests to compute
readiness gates. The fix was a narrower domain boundary, not abandoning
the slice: `ComplianceDomain` (`services/api/src/cp2/domains/compliance/`)
owns the eleven Maps and every CRUD/validation/event method that reads or
writes them directly, plus public `getOrCreate*`/`*ForBusiness` accessors
for its own records. The three readiness-report builders and
`auditEventsForBusiness` stayed on `Cp2Store` since they are cross-cutting
report-engine logic that happens to read this domain's records, not
domain logic itself - moving them would have just relocated the ambient
coupling instead of removing it. **Read the method bodies, not just the
Map names, before estimating a future row's coupling** - a Map that looks
self-contained can still be read by a report builder that isn't.

| Order | Domain | Maps | Coupling | Notes |
|---|---|---|---|---|
| 1 | **Compliance/Beta/Launch** ✅ done | `verificationTiers`, `taxConfigs`, `deviceTrust`, `betaAccess`, `betaFeatureFlags`, `betaDeviceTests`, `betaSupportTickets`, `betaTelemetryEvents`, `launchSettings`, `launchChecklist`, `launchIncidents` | Medium (see correction above) | `services/api/src/cp2/domains/compliance/`. Readiness-report builders and `auditEventsForBusiness` deliberately stayed on `Cp2Store` - see the correction note above for why. Verified: full suite unchanged (657/15/1), live-Postgres persistence re-verified for all 11 tables in `tests/cp2-postgres-store.test.ts`. |
| 2a | **Logistics** ✅ done | `logistics`, `logisticsByInvoice` | Low (confirmed against method bodies) | `services/api/src/cp2/domains/logistics/`. Deps: `requireAuthorizedSession`, `appendBusinessEvent`, `requireInvoice`. `logisticsByInvoice` is a derived invoice-id -> logistics-id index, never a `Cp2Snapshot` field - rebuilt per-item during `hydrateSnapshot` and via a dedicated `rebuildLogisticsByInvoiceIndex()` method for the account-purge derived-index rebuild path. `summarizeLogistics` moved to the domain's `shared.ts` and is imported back into `store.ts` for the two report builders (`buildRuntimeContext`, `buildBusinessReport`) that still need it. Verified: full suite unchanged (657/16/1, +1 skipped from the new postgres test), live-Postgres persistence re-verified including the derived-index rebuild (a second `POST /logistics` for the same invoice must still 409 after a restart). |
| 2b/3 | **Suppliers, sales agents, and purchase receipts/receipt OCR** ✅ done | `suppliers`, `salesAgents`, `supplierContactLinks`, `purchaseReceipts`, `receiptLineItems`, `receiptOCRJobs` | High (confirmed) | `services/api/src/cp2/domains/suppliers/`. Extracted as one combined domain, not two - `createReceiptOCRJob`/`confirmReceiptOCRJob` call `matchSupplier`/`requireSupplier`/`createSupplier`/`matchSalesAgent`/`requireSalesAgent`/`createSalesAgent`, while `supplierBusinessCard`/`salesAgentCard` call back into `purchaseReceiptsForSupplier`/`purchaseReceiptsForSalesAgent` - splitting them would have recreated the same cross-module circular-dependency problem `cp2-error.ts`/`text-normalization.ts`/`money.ts` were pulled out to avoid. Needs `networkNodes`/`networkSources` (phonebook contact-matching) injected as raw Map references, same as `CommerceDomainDeps` did for `networkNodes`; `sanitizeNetworkNode` injected as a function since it's also used by the not-yet-extracted network-graph domain. Two more supporting extractions this slice needed: `money.ts` (`roundMoney`, used by the receipt-parsing block and 20+ other call sites) and moving `normalizeDestination` into `phone-identity.ts` (it belongs there conceptually and was already store.ts-exported for `tests/phone-identity.test.ts`). Verified: full suite unchanged (657/17/1), live-Postgres persistence re-verified for suppliers/sales-agents/purchase-receipts including the receipt-OCR-confirm flow and the derived `salesAgentCount`/`purchaseReceiptCount` card fields surviving a restart. |
| 4 | **Document imports** ✅ done | `documentImports`, `documentImportSources` | Medium (confirmed) | `services/api/src/cp2/domains/document-imports/`. `confirmSupplierImport`/`confirmProductImport` call back up into `Cp2Store.createSupplier`/`Cp2Store.createProduct` (still real methods there, not yet extracted) via injected deps functions - same "call back up" pattern `LogisticsDomainDeps.requireInvoice` uses. The binary-upload-pipeline note was previously imprecise: that dependency lives entirely in `routes.ts` (constructed from `registerCp2Routes` options), never in `store.ts` - the domain only ever receives an already-extracted `DocumentImportSourceInput`, so it needed zero pipeline wiring. Verified: full suite unchanged (657/18/1), live-Postgres persistence re-verified for the supplier-CSV-import-to-confirm flow. |
| 5 | **Notifications** ✅ done | `notifications`, `notificationByRuleKey` | Low (this table's own "Medium/needs event-observer-design" estimate was wrong - see below) | `services/api/src/cp2/domains/notifications/`. This row's original guess - "read-heavy from almost every other domain, needs an event/observer boundary" - turned out false once the call sites were actually read: nothing in any of the four previously-extracted domains writes a notification. Every write happens inside `Cp2Store` itself, through exactly two paths (`recordSecurityNotification` from the shop-deletion flow, and the lazy `ensureDeterministicNotifications` recompute triggered by `buildBusinessKnowledge`), so a plain constructor-injected `buildBusinessReport` callback was sufficient - no event bus needed. **Found and fixed a real pre-existing bug in the process**: the derived-index rebuild (on snapshot hydrate and after an account purge) recomputed `notificationByRuleKey` as `businessId:type`, but no notification is ever written under that key - the seven deterministic rules each use a distinct hand-picked dotted ruleKey (e.g. `beta.readiness`, not `beta_readiness`) and security notifications use `businessId:type:sourceId`. The rebuilt index therefore never matched a real entry, so every store restart or account purge silently duplicated all seven deterministic notifications the next time they were recomputed. The new live-Postgres test caught this directly (restart produced 4 notifications instead of 2, not a snapshot round-trip mismatch) - fixed by reconstructing the exact original ruleKey from already-persisted fields (`notificationRuleKey` in the domain's `shared.ts`), used at every rebuild site, not patched over. Verified: full suite unchanged (657/19/1), live-Postgres persistence re-verified including the fixed rule-key rebuild (a second restart-triggered recompute must not duplicate the two deterministic notifications). |
| 6 | **Network/contacts graph** ✅ done | `networkNodes`, `networkEdges`, `networkSources`, `networkPermissions`, `networkRoutes`, `contactHashes` (+ derived `contactHashIdByValue`), `externalIdentities` (+ derived `externalIdentityIdBySubject`), `sokoIdentityLinks` | High, but almost entirely one-directional (confirmed) | `services/api/src/cp2/domains/network/`. Unlike every prior slice, this domain is **user-scoped, not business-scoped** - its public methods gate on `requirePinVerifiedSession`, never take a `businessId`. The coupling ran opposite to the usual shape too: instead of cross-cutting report builders reaching in, this domain's own methods (`syncConnectedSocialProvider`, `findSokoIdentityLink`) reach into the not-yet-extracted core auth/identity kernel (`accounts`, `userByAccount`, `memberships`, `businesses`, `userIdentities`), injected as read-only raw Map references. The commerce and suppliers domains (already extracted) kept working with zero code changes - both only ever read `networkNodes`/`networkSources`, never write, so `Cp2Store`'s constructor just re-points their existing `networkNodes`/`networkSources` deps at `this.networkDomain.networkNodesMap`/`networkSourcesMap` instead of its own former private fields. `sanitizeNetworkNode` (no `this` dependency) moved to the domain's `shared.ts` and is imported directly by `SupplierDomain`'s own file rather than re-injected as a closure. `requirePhonebookNode` moved here too (it only operated on `networkNodes`) and is still injected into `SupplierDomainDeps`, just re-pointed. `createContactHash` stays re-exported from `store.ts` for `tests/network-sync-graph.test.ts`'s direct import. Unlike row 5's `notificationByRuleKey` bug, this domain's two derived indexes (`contactHashIdByValue`/`externalIdentityIdBySubject`) were already rebuilt with the exact same key format used at creation time in both `hydrateSnapshot` and `rebuildDerivedIndexesAfterAccountPurge` - checked specifically because of what row 5 found, and confirmed clean here. Verified: full suite unchanged (657/20/1), live-Postgres persistence re-verified including the `contactHashIdByValue` index surviving a restart (re-syncing the same phone number afterward reuses the original contact-hash id instead of minting a duplicate). |
| 7 | **Messaging & channels** ✅ done | `conversations`, `conversationParticipants`, `conversationMessages`, `platformIdentities`, `conversationChannels`, `providerUpdateReceipts`, `channelIdentityLinkGrants`, `nativeSmsDevices`, `nativeSmsDeviceCommands`, `connectedMailboxes`, `connectedMailboxOAuthSessions`, `customerRuntimeCapabilities`, `messageDeliveryAttempts`, `messageNotificationDeliveries`, `e2eeDevices`, `pushSubscriptions` (+ derived `pushSubscriptionIdByEndpoint`), `conversationTyping`, `messageByClientId`, `messageByIdempotencyKey` | High (confirmed - the largest slice yet, 96 methods) | `services/api/src/cp2/domains/messaging/`. `customerRuntimeCapabilities` and `pushSubscriptions`/`pushSubscriptionIdByEndpoint` weren't in this row's original Map list - reading the method bodies showed they're exclusively messaging/storefront infrastructure, so they moved here too. The E2EE invariant this row warned about (`validateConversationEncryption`) moved verbatim, character-for-character verified against the pre-extraction original, with its own regression test (`tests/cp20-unified-session-conversations.test.ts`) run explicitly rather than trusted to the full-suite pass/fail count alone, given the stakes. Agent/AI runtime coupling (`createRuntimeTurn`, `agentModelRecoveryGuidance`) injected as callbacks, same "call back up" pattern as every other not-yet-extracted dependency in this table. `attemptPublicAgentReply` deliberately stayed on `Cp2Store` rather than moving - it depends on six separate agent-runtime primitives that will all move together when row 8 is eventually extracted, so injecting them piecemeal now would just create churn on both sides of that future PR; it's injected as a single callback instead, and calls back into this domain's `persistExternalConversationMessage`/`persistExternalProductCard`/`publicMessageView`/`requireCustomerCapability`, which is why those four became deliberately public. **The dependency-mapping research for this row (done via a research agent, same as every other row) still under-reported real coupling** - actual extraction found four more external call sites only visible by grepping the transformed file against a live typecheck: `requireAccountConversation`/`ensurePersonalAccountConversation` are called from core session-creation code (`createSession`, `ensureSokoSessionContext`) and had to become public accessors too, and `backfillSyncChanges` (a core sync-kernel method) reads `conversations`/`conversationMessages` directly. None of this was caught by the research phase; only the mechanical typecheck-fix loop caught it. Confirms the standing lesson: treat the research report as a strong starting map, not a complete one - a clean typecheck after the mechanical move is the actual proof of completeness, not the research. Verified: full suite unchanged (657 passed/21 skipped/1 pre-existing failure), live-Postgres persistence re-verified for conversations/messages/push-subscriptions including both derived-index rebuilds surviving a restart (re-posting an identical message and re-registering an identical push endpoint after restart both upsert onto the original record rather than duplicating). |
| 8 | **Agent/AI runtime** ✅ done | `activeAiModels`, `agentProfiles`, `agentRuntimeVersions`, `agentContextSources`, `agentEvaluationEvents`, `agentOwnerCorrections`, `installedAgentModels`, `agentModelAssignments`, `browserInferenceAssignments`, `agentModelBindings` (+ the ephemeral `agentModelActivationLocks` mutex `Set`), `runtimeSessions`, `runtimeTurns`, `pendingRuntimeActions` (never persisted - no `Cp2Snapshot` field) | High (confirmed - the largest slice by method count and line volume: 70 methods, 35 public delegators) | `services/api/src/cp2/domains/agent-runtime/`. **`mcpAccessTokens`/`mcpTokenIdByHash` deliberately stayed on `Cp2Store`, not here** despite being declared next to this domain's Maps in the old field list - zero code coupling either direction, a separate route module (`services/api/src/mcp/routes.ts`), and a separate purge-script batch. **`attemptPublicAgentReply` also stayed on `Cp2Store`** (as row 7 predicted) - it turned out to depend on *seven* agent-runtime primitives, not six as documented when messaging was extracted (`createCustomerCatalogueRuntimeTurn` was missing from that earlier count, caught by this row's own dependency-mapping research). Rather than reshape `MessagingDomainDeps` to inject all seven individually, nine of `AgentRuntimeDomain`'s otherwise-private methods became deliberately public accessors (mirroring `ComplianceDomain`'s `getOrCreate*` pattern), and `Cp2Store`'s `attemptPublicAgentReply`/`buildShopDeletionPreview`/messaging-constructor-wiring now call `this.agentRuntimeDomain.X(...)` directly. **Known pre-existing gap, preserved as-is, not silently fixed:** `deleteShopOwnedData` (business-scoped deletion) only ever swept 4 of these 12 Maps (`browserInferenceAssignments`/`runtimeSessions`/`runtimeTurns`/`pendingRuntimeActions`) - the other 9 are only cleared by the account-level purge. This was true before the extraction; flagged here (and in the domain's own header comment) rather than fixed as an incidental side effect, since shop-deletion semantics for business-scoped agent config deserve a deliberate product decision. Pure helpers split into the domain's `shared.ts` as usual (68 free functions/consts including the AI model catalog `aiModelRegistry`, 4 profile interfaces); two functions needed by both moving and staying code (`readBoundedSecurityInteger`, `isSupportedLanguage`) went to the genuinely-shared `text-normalization.ts` instead. **A large single-file first draft (6,114 lines) was deliberately rejected mid-task** for violating every other domain's `shared.ts`/`store.ts` split, and rebuilt split - a reminder that "it compiles" isn't the bar, "matches the established pattern" is. Two real test regressions - caused by this extraction, not pre-existing - surfaced only by the full suite, not by typecheck: `tests/recall-distillation.test.ts` monkey-patched `store.persistRecallCandidate` directly (now `store["agentRuntimeDomain"].persistRecallCandidate`, since the method moved), and `tests/frontend-user-guidance.test.ts` asserted the AI model catalog's string literals lived in `store.ts` by file path (now asserts against `domains/agent-runtime/shared.ts`) - both fixed as part of this slice, not deferred. Verified: full suite unchanged (657 passed/23 skipped/1 pre-existing failure, +1 skipped from the new postgres test), live-Postgres persistence re-verified for agent profiles, context sources, owner corrections, evaluation events, installed models, model assignments, and browser inference assignments (the pre-existing binding/activation/runtime-turn persistence test already covered `activeAiModels`/`agentModelBindings`/`runtimeTurns`, so the new test targeted everything that test didn't). |
| 9 | **Invoicing/products/customers ("Sales")** ✅ done | `products`, `productMedia`, `productFieldSchemas`, `customers`, `invoices`, `payments`, `inventoryMovements`, `publicOrders`, `publicCustomerCareRequests`, `publicStorefrontMessages` (dead weight - see below), + derived `nextInvoiceNumberByBusiness` (never a `Cp2Snapshot` field, rebuilt from `invoices` on restore, same pattern as `logisticsByInvoice`) | High (confirmed - the most heavily cross-coupled slice yet, more so than this table's framing suggested) | `services/api/src/cp2/domains/sales/` (named `sales`, not `commerce`, to stay distinct from the already-extracted `domains/commerce/`, which owns the storefront/social-commerce surface - product capture jobs, status broadcasts, buy orders, unified checkouts - built on top of this deeper transactional core). 43 methods (26 public delegators, 17 formerly-private, 15 of those made public). **Unlike every domain before it, three already-extracted domains held raw, sometimes-*mutable* references into this cluster's Maps, not read-only callbacks**: `CommerceDomain` reads/writes `products`/`productMedia` directly (capture-job photo pipeline) and writes `invoices` directly (`createUnifiedCheckout`); `MessagingDomain` reads and writes `customers` directly (phone/email inbound-message matching); `AgentRuntimeDomain` reads `products`/`customers`/`invoices` read-only. All three now hold mutable getters onto `SalesDomain`'s Maps instead, re-pointed with zero code change on their own side - the same trick used for `NetworkDomain`'s `networkNodes`/`networkSources`, except three of these four had to stay mutable rather than read-only. **A genuine circular-construction-order conflict, resolved by converting one side to callbacks**: `SalesDomain` needed `MessagingDomain.platformIdentitiesMap` (for `linkCustomerAccount`/`createPublicOrder`), while `MessagingDomain` needed `SalesDomain.customersMap` - both as raw Maps, which cannot both be true at constructor time no matter the ordering. Fixed by converting `SalesDomain`'s `platformIdentities` dependency from a raw Map to two callbacks (`findPlatformIdentity`, `relinkPlatformIdentitiesForCustomer`) - lazy closures don't care about construction order, only raw Map references do. `SalesDomain` is now constructed first, before every other domain. Fifteen otherwise-private methods became public accessors, all for the same reason row 1 (Compliance) established: the cross-cutting readiness/report builders that stayed on `Cp2Store` (`buildRuntimeContext`, `buildComplianceReport`, `buildBetaReadinessReport`, `buildLaunchReadinessReport`, `buildBusinessReport`, `buildBusinessKnowledge`, `buildOfflineCacheSnapshot`, `buildShopDeletionPreview`, `publicStorefrontForBusiness`, `createDataExport`) reach into nearly every domain to compute readiness gates, and moving them would just relocate the ambient coupling. A genuine research gap this row's own dependency-mapping missed: `previewInvoice` (a public `Cp2Store` method never mentioned by name in the original Map-based research) was found only by directly grepping every `this.<mapName>` reference across the whole file rather than trusting the method list - confirms the standing lesson from rows 7 and 8 that research reports are a strong starting map, not a complete one. **`publicStorefrontMessages` turned out to be dead code** - exhaustive grep found zero business-logic reads or writes anywhere in the codebase; the real "public storefront message" feature routes actually call is implemented entirely inside `MessagingDomain` as a computed view over `conversationMessages`, under the same TypeScript type name by pure coincidence. `scripts/purge-all-users.sql` independently labels the Postgres table "Legacy," confirming this was already known-stale, not a fresh find - it moved into this domain as inert weight, not wired up to anything. **Two known pre-existing gaps, preserved as-is, not silently fixed** (same "flag, don't fix" precedent as row 8): `deleteShopOwnedData` never sweeps `productMedia` even though every other businessId-scoped Map in this cluster gets an explicit loop there, so a shop deletion leaves orphaned `productMedia` rows until the next full account purge; and `nextInvoiceNumberByBusiness` is never cleared by any purge path (low severity - business IDs are UUIDs never reused, so this can never cause a duplicate invoice number - but asymmetric with how every other derived index in this codebase is explicitly rebuilt after purge). Unlike rows 7 and 8, the full suite stayed green on the first post-extraction run - no test regressions surfaced this time, worth flagging as a deliberate contrast since not every extraction produces one. Verified: full suite unchanged (657 passed/24 skipped/1 pre-existing failure, +1 skipped from the new postgres test), live-Postgres persistence re-verified for customers, invoices (including the confirm-invoice inventory-movement side effect), payments, product field schemas, public customer-care requests, and public orders (the pre-existing "persists API state" test already covered basic product creation, so the new test targeted everything that test didn't, including the full public-storefront-session-to-order flow through `MessagingDomain`'s capability-token issuance). |
| 10 | **MCP access tokens** ✅ done | `mcpAccessTokens` + derived `mcpTokenIdByHash` | Low (confirmed - the smallest, most self-contained slice yet) | `services/api/src/cp2/domains/mcp-tokens/` (named `mcp-tokens`, not `mcp`, to stay distinct from the pre-existing top-level `services/api/src/mcp/` route directory it serves). 4 methods, all public delegators. Exactly matches the assessment already made when `AgentRuntimeDomain` was extracted: zero code coupling with any other domain, its own dedicated route module, its own purge-script batch. **Found a genuine pre-existing gap while reading the code, preserved as-is rather than fixed**: `hydrateSnapshot()` never cleared `mcpAccessTokens` before restoring a snapshot into it, unlike every other Map in the store - an in-process restore (not the normal single-restore-at-startup path) would leave stale tokens from the prior in-memory state merged with the new snapshot instead of fully replaced. The new domain's own `clear()` correctly clears it (needed for the account-purge path), but `Cp2Store.hydrateSnapshot()` deliberately does not call it, matching the pre-extraction behavior exactly. No new live-Postgres test needed - the existing "persists API state" test already creates an MCP token, restarts the store, and re-authenticates it via `authenticateMcpAccessToken` on the restored instance, which is exactly this domain's persistence path; re-run and confirmed still green after the extraction. Verified: full suite unchanged (657 passed/24 skipped/1 pre-existing failure, no new test added so the skip count is unchanged from row 9). |
| 11 | **Passkeys** ✅ done | `passkeys`, `passkeyCeremonies`, `passkeyPinRecoveryGrants` | Medium (confirmed - the first slice successfully carved out of the "core auth/identity/session kernel" this table originally flagged as too large/risky to touch) | `services/api/src/cp2/domains/passkeys/`. 8 public delegators + 3 private-only helpers. **The core risk going in - does a passkey ceremony ever write `sessions`/`accounts` directly - was investigated, not assumed, and the answer is no**: `completePasskeyAuthentication` creates its session via a `createSession` callback, the exact "call back up" pattern used throughout this whole effort, confirmed by reading the method body directly rather than inferring from its name. The one real coupling problem was `recoverPhoneAccountPinWithPasskey`, which raw-iterated `sessions` to revoke every other session for the account and raw-read/wrote `accountPinHashes` to reset the login PIN - both patterns already duplicated inline in several `Cp2Store`-only methods (`changePassword`, `resetRecoveredPassword`, `revokeSessionsForAccount`), confirming `sessions`/`accountPinHashes` are core-kernel state that legitimately stays put. Resolved with four small new callbacks (`revokeOtherSessionsForAccount`, `resetAccountPinHash`, `hasAccountPinHash`, `hasPasswordCredential`) - `Cp2Store` implements each as a one-line wrapper around Maps it already owns, no different in kind from every other domain's injected callbacks. `normalizePin` is also injected as a callback (rather than value-imported back from `store.ts`, which would recreate the exact circular-value-import problem `cp2-error.ts`/`money.ts`/`text-normalization.ts` were split out to avoid) specifically so `recoverPhoneAccountPinWithPasskey` keeps validating the PIN format at the same point in its control flow as before the extraction - hashing it lazily inside a single combined callback instead would have silently reordered which error wins when both the PIN and the session are invalid. **Known pre-existing gap, preserved as-is, not silently fixed:** `passkeyPinRecoveryGrants` is never swept by `deleteAccountOwnedData` - low severity, since it is a bare `Map<sessionId, expiresAt>` the generic sweep helper doesn't apply to unmodified anyway, and `prunePasskeyPinRecoveryGrants` already self-heals once the underlying session is purged, on top of the 5-minute TTL bounding the exposure. **A new live-Postgres persistence test needed an extra step other rows didn't**: `hydrateSnapshot()` bypasses the Postgres persistence proxy's method-interception entirely (it only enqueues a save for an explicit `mutatingMethodNames` allowlist), so injecting a synthetic passkey via `store.hydrateSnapshot()` alone left it in-memory-only; fixed by following the injection with a real call to `renamePasskey` (already on that allowlist), which triggers a full-snapshot persist that picks up the current in-memory state, passkey included - a technique worth reusing for any future domain that needs to seed hard-to-construct fixtures (WebAuthn credentials can't be created through the real flow without simulating browser-side cryptography) into a live-Postgres test. Verified: full suite unchanged (657 passed/25 skipped/1 pre-existing failure, +1 skipped from the new postgres test), live-Postgres persistence re-verified for a registered passkey credential surviving a restart (the pre-existing "persists passkey ceremony creation" test already covered `passkeyCeremonies` via its dedicated targeted-write fast path, and `tests/passkeys.test.ts` already covers the full registration-options/authentication/PIN-recovery flow at the unit level with an injected mock verifier, so the new test targeted the one persistence gap neither covered: an actual `passkeys` credential surviving a restart). |
| 12 | **OAuth/identity linking** ✅ done | `userIdentities`, `identityByProviderSubject`, `identityByEmail`, `oauthSessions` | Medium (confirmed - the second slice carved out of the "core auth/identity/session kernel") | `services/api/src/cp2/domains/oauth/`. 9 public delegators + 3 private-only helpers (one, `findIdentityByVerifiedEmail`, made public rather than private since `completeOtpVerification` - still on `Cp2Store`, part of the not-yet-extracted OTP cluster - calls it directly to avoid creating a duplicate account when an OTP-verified email is already linked as an OAuth identity, the same kind of reverse coupling a Map-name-only grep would miss). **The core risk going in - does OAuth login completion write `sessions`/`accounts` directly - was investigated, not assumed, and the answer is no**, matching row 11's finding for passkeys exactly: `completeOAuthProfileAuthentication` resolves/creates the account via `createAccount`/`requireAccount` callbacks and produces the session via a `createSession` callback. The real coupling was three raw touches into core-kernel Maps this cluster doesn't own (`userByAccount` read, `users` write, `accountByDestination` write) plus the `accountPinHashes` last-login-method guard already solved by row 11's `hasAccountPinHash` callback - all four resolved the same way row 11 resolved its own core-kernel coupling: small callbacks `Cp2Store` implements as one-line wrappers around Maps it already owns (`userForAccount`, `updateUserDisplayName`, `linkEmailAccountDestination`). A genuine circular-construction-order conflict, resolved by re-ordering rather than converting to callbacks (a cheaper fix than row 9 needed): `NetworkDomain` needs `OAuthDomain.userIdentitiesMap` as a raw ref, and `OAuthDomain` itself has zero raw-Map dependencies on any other domain (only closures) - so `OAuthDomain` simply had to construct before `NetworkDomain`, no Map-to-callback conversion needed. `deleteShopOwnedData` (business-scoped, stays on `Cp2Store`) redacts encrypted OAuth tokens on `userIdentities` entries belonging to the account whose shop is being deleted, even though `userIdentities` is account-scoped, not shop-scoped - a pre-existing defense-in-depth step, preserved verbatim via a mutable `userIdentitiesMap` getter. `otpTtlMs` moved to the genuinely-shared `text-normalization.ts` rather than either domain's `shared.ts`, since `beginOAuthSession` (this cluster) and `requestOtp` (the not-yet-extracted OTP cluster) both need it - the same class of gap row 8 hit for `readBoundedSecurityInteger`/`isSupportedLanguage`. **Found and fixed a real, severe pre-existing production bug in the process, not just flagged**: `snapshot()` was serializing `userIdentities`/`oauthSessions` through the public-API view mappers (`userIdentityView`/`oauthSessionView`), which strip `stateHash`, `csrfHash`, `codeChallenge`, `codeVerifier`, `redirectUri`, and the encrypted OAuth tokens down to the public summary shape - unlike every other domain's snapshot field (e.g. `passkeys: [...this.passkeyDomain.passkeysMap.values()]`, always the full record, never a stripped view). Confirmed via `git show` against the pre-extraction commit that this bug predates this whole modularization effort entirely - every Postgres flush has been silently writing `state_hash = ''` for every OAuth session in the product's history, meaning OAuth login-session recovery across a server restart has never actually worked. `restore()`'s defensive `Partial<Pick<...>>` casting with `?? ""`/`?? null` fallbacks (needed to tolerate rows already corrupted in the live DB by this bug) was the tell that led to finding it. Fixed by changing `Cp2Snapshot.userIdentities`/`oauthSessions` from the public `*Summary[]` types to the full `UserIdentityRecord[]`/`OAuthSessionRecord[]` types and having `snapshot()` emit the raw Map values directly, matching every other domain's pattern; the view mappers stay in use for their legitimate purpose (`OAuthCallbackResult`'s public API response). **Found and fixed a second real, severe pre-existing bug surfaced only by writing the live-Postgres test**: `NetworkDomain.findSokoIdentityLink` (row 6, already-extracted) computed a contact hash's expected channel (`"email"` or `"phone"`) from the *contact* being matched, then blindly called `createContactHash(channel, account.primaryAuthDestination)` for *every* account in the system regardless of that account's own destination type - so the instant any OAuth/email-authenticated account existed anywhere in the store, every phone-contact sync for every user crashed with an uncaught `INVALID_PHONE_NUMBER` error the moment the scan reached that account (email string fed through phone parsing). This had been a landmine since row 6 was extracted, invisible until this row's own Postgres test became the first thing in the reset dev database to create an email-primary-destination account, which then broke two unrelated pre-existing tests (`persists status broadcasts and unified checkout orders`, `persists the network contact graph`) that both call `/network/sync/contacts`. Fixed by checking `account.primaryAuthChannel === channel` (a field that already existed on `AccountSummary` for exactly this purpose) before attempting the hash comparison, instead of forcing the wrong channel and crashing. Verified: full suite unchanged (657 passed/26 skipped/1 pre-existing failure, +1 skipped from the new postgres test), live-Postgres persistence re-verified for an OAuth-linked identity and OAuth session surviving a restart (including the exchange-data lookup, which would have silently failed forever without the snapshot fix), and the two previously-crashing network-sync tests re-verified green after the `findSokoIdentityLink` fix. |
| 13 | **OTP verification** ✅ done | `otpChallenges`, `smsDeliveryAttempts`, `otpRequestHistory` | Medium (confirmed - the third slice carved out of the "core auth/identity/session kernel") | `services/api/src/cp2/domains/otp/`. 5 public delegators (`requestOtp`, `verifyOtp`, `getOtpChallengeDelivery`, `getOtpChallengeDeliveryByContact`, `verifyExternallyApprovedOtp` - the last one unwired to any route but referenced by `postgres-store.ts`'s `mutatingMethodNames` allowlist, carried over as-is) + 5 private helpers + 3 fully dead private helpers (`verifyOtpCodeOnly`, `markOtpCodeExternallyVerified`, `getDeletionOtpDelivery` - confirmed zero callers anywhere in the codebase, carried over as inert weight rather than deleted, the same "flag, don't unilaterally redesign" precedent row 9 set for `publicStorefrontMessages`). `otpRequestHistory` is never a `Cp2Snapshot` field and never swept by any purge path - a purely in-memory rate-limit tracker keyed by request strings, not entity IDs, so `deleteScopedMapRecords` structurally couldn't touch it even if wired in; unchanged pre-existing behavior. **The core risk - does OTP completion write `sessions`/`accounts` directly - was investigated, not assumed, and the answer is no**, matching rows 11 and 12 exactly: `completeOtpVerification` resolves-or-creates the account via `createAccount`/`requireAccount` and produces the session via `createSession`, the standard "call back up" pattern. **The one genuinely new wrinkle this row hit**: three methods elsewhere on `Cp2Store` (`verifyEmailRecovery`, `verifyEmailIdentityMerge`, `verifyPendingEmail` - part of the email-recovery/identity-merge/pending-email-verification cluster, not OTP itself) read and mutate `otpChallenges` records directly, with their own inlined validation checks that are subtly different from `validateOtpChallenge` (they additionally compare the challenge's `purpose`/`channel`/`destination` against the caller's own transaction). Rather than force those three through new OTP-domain primitives that would have to faithfully replicate their already-divergent rules - real design work, not mechanical extraction - they keep raw mutation access via a `otpChallengesMap` getter, the same escape hatch already used for `deleteShopOwnedData`'s OAuth-token redaction (row 12) and the mutable getters `CommerceDomain`/`MessagingDomain` hold onto `SalesDomain`'s Maps (row 9). **Found and cleaned up dead duplicate code while extracting, not introduced by this row**: `hashOtp`/`otpHmacSecret` and `readBoundedSecurityInteger` each had two copies - a shared one already in `text-normalization.ts` (added by earlier rows for the same "needed by both moving and staying code" reason) and a second, stale local copy still sitting in `store.ts` that its own remaining callers kept using instead of the shared one. Extracting this row's last local caller of each made the local copies fully dead (caught by the same eslint `no-unused-vars` pass every row runs), so they were deleted in favor of the single shared definition rather than left as orphaned duplicates. `hashMatches` and `pinAttemptTrackerMaximumEntries` got the same treatment - genuinely shared between this domain (moving) and `failedPinAttempts`/legacy-PIN-hash comparison (staying) - moved to `text-normalization.ts` alongside `otpTtlMs`, which already lived there for the identical reason. `VerifyOtpResult` moved into this domain's `shared.ts` (its natural owner) with `domains/oauth/store.ts`'s existing type-only import re-pointed from `../../store.js` to `../otp/shared.js`. Verified: full suite unchanged (657 passed/27 skipped/1 pre-existing failure, +1 skipped from the new postgres test), live-Postgres persistence re-verified for an OTP challenge requested, surviving a restart while still unverified, and completing successfully against the restored store (proving `codeHash` and the rest of the challenge record round-trip correctly, not just the container fields) - the new test also caught that `/auth/accounts` (`listLoginAccounts`, OAuth-specific) and unauthenticated-phone business creation aren't valid verification targets for a pure email-OTP account, settling on `/auth/session` as the correct account/session liveness check instead. |
| 14 | **Device bootstrap/recovery** ✅ done | `deviceAccountBootstraps`, `deviceAccountBootstrapCredentials`, `deviceRecoveryCredentials`, `deviceRecoverySessionCredentials` | Medium (confirmed - the fourth and final slice carved out of the "core auth/identity/session kernel") | `services/api/src/cp2/domains/device-bootstrap/`. 3 public delegators (`continueWithDevice`, `recoverWithDeviceCredential`, `mergeCurrentDeviceAccountWithPin`) + 2 private helpers (`ensureDeviceRecoveryCredential`, `pruneDeviceAccountBootstraps`). `deviceAccountBootstrapCredentials`/`deviceRecoverySessionCredentials` are deliberately never `Cp2Snapshot` fields (both hold plaintext refresh-token replay caches that shouldn't survive a restart, unchanged from the pre-extraction monolith). **The core risk - does device bootstrap/recovery completion write `sessions`/`accounts` directly - was investigated, not assumed, and the answer is no** for both session-issuing entry points, the standard "call back up" pattern one more time. **`mergeDeviceAccountData` deliberately stayed on `Cp2Store`, not moved into this domain**, despite being called by `mergeCurrentDeviceAccountWithPin` - reading its body shows it never touches any of this domain's four Maps directly; it performs a whole-store `snapshot()` -> filter/rewrite -> `hydrateSnapshot()` cycle to graft one account's session/PIN state onto another, which is generic core-kernel functionality also called by `verifyEmailIdentityMerge` (a different, not-yet-extracted identity-merge cluster) - confirming it isn't this domain's to own. Moving it in would have given the domain a "reach into the entire store" dependency unlike every other domain's narrow callback list; injecting it as one callback instead keeps the dependency surface the same shape as every prior row's. **Two reverse cross-references found and resolved the same way rows 12/13 resolved theirs**: `logoutAll` (core session-revocation, stays on `Cp2Store`) iterates and mutates `deviceRecoveryCredentials`/`deviceRecoverySessionCredentials` directly when revoking every session for an account; `verifyEmailIdentityMerge` (not-yet-extracted identity-merge cluster) re-points a `deviceAccountBootstraps` entry's `sessionId` after a merge. Both keep raw access via `deviceRecoveryCredentialsMap`/`deviceRecoverySessionCredentialsMap`/`deviceAccountBootstrapsMap` getters, the same escape-hatch pattern as `otpChallengesMap` (row 13) and OAuth's `userIdentitiesMap` (row 12). `mergeCurrentDeviceAccountWithPin`'s PIN-verification path needed the same core-kernel callback set passkeys/OTP already established (`accountPinHashes`, `failedPinAttempts`, `requirePinAttemptAllowed`, `recordFailedPinAttempt`, `verifyStoredPin`) plus one new one-line callback, `simulateFailedPinCheck` - wraps the existing `verifyPinHash("unknown-account", pin, dummyPinHash)` constant-time dummy-hash comparison that runs even when no account matched, preserving the timing-attack mitigation exactly. `normalizePin` injected as a callback rather than value-imported, same reasoning as row 11 (avoids a circular value-import back into `store.ts`, preserves exact error-ordering). No new live-Postgres test needed - `tests/progressive-identity-postgres.test.ts` already covered this cluster thoroughly (idempotent bootstrap replay surviving a restart with direct SQL assertions against `cp2_device_account_bootstraps`/`cp2_device_recovery_credentials`, plus a full proof-gated device-to-target-account merge surviving a restart with `deviceRecoveryCredentials` correctly re-pointed) - re-run and confirmed still green after the extraction, the same "existing coverage is sufficient" call row 10 made for MCP tokens. Verified: full suite unchanged (657 passed/27 skipped/1 pre-existing failure, no new test added so the skip count is unchanged from row 13), live-Postgres persistence re-verified via the existing dedicated test file. **This was also the last candidate row on this table** - every other Map still on `Cp2Store` (`accounts`, `users`, `sessions`, `sessionContexts`, `businesses`, `memberships`, `accountPinHashes`, `failedPinAttempts`, `mfaFactors`, `recoveryCodes`, `passwordCredentials`, `authTransactions`, `accountIdentities`, `identityAccountByValue`, `pendingRefreshTokens`, `recentSessionRotations`, `syncQueue`, `syncChanges`, `accountDeletionRequests`, `accountDeletionProofs`, `dataExports`, `auditEvents`, plus every cross-cutting readiness/report builder and `mergeDeviceAccountData` itself) is now genuinely irreducible core, not an unresearched placeholder - each was investigated as a byproduct of extracting the fourteen rows above it, not assumed. |
| — | **Irreducible core** (not a row - the permanent composition root) | `accounts`, `users`, `sessions`, `sessionContexts`, `businesses`, `memberships`, `accountPinHashes`, `failedPinAttempts`, `mfaFactors`, `recoveryCodes`, `passwordCredentials`, `authTransactions`, `accountIdentities`, `identityAccountByValue`, `pendingRefreshTokens`, `recentSessionRotations`, `syncQueue`, `syncChanges`, `syncChangeListeners`, `accountDeletionRequests`, `accountDeletionProofs`, `dataExports`, `auditEvents`, plus every cross-cutting readiness/report builder | N/A | `requireAuthorizedSession`/`requirePinVerifiedSession` themselves and the account/user/business/membership records they gate against cannot move without becoming a differently-named copy of the same composition root every other domain already calls back into - there is no coupling left to remove by relocating them. Whether rows 11-14 clear the bar for their own extraction is still being determined per-row, not assumed.

## Ground rule for every future slice

One domain per PR. Full test suite must stay at the same pass/skip/fail
count before and after (currently 657 passed / 25 skipped / 1 pre-existing
unrelated failure - the migration-051 checksum test). Re-run the live-
Postgres persistence test for that domain's tables specifically, the same
way this slice did, before calling it done - the in-memory test suite
alone will not catch a missed `normalizedCollections`/`emptySnapshot()`/
purge-script entry, all three of which are silent failures that only show
up against a real database or a real account-deletion request.
